// Create an object person with properties like name and age.
// Then create a new object `student` that inherits from person and has an additional property `studentId`.
// Add a method to the person object and demonstrate that `student` also has access to it.